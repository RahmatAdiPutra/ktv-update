(function () {
    let ROOM_KEY = localStorage.getItem('ECHO_ROOM_KEY');
    let room = null;

    // cek apakah room_key sudah di set
    if (ROOM_KEY) {

    }

    localStorage.setItem('ECHO_ROOM_KEY', 'Tom');
})();
